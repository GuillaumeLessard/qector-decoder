// QECTOR Decoder v3 -- Rust core (proprietary, not distributed in source form).
// Pre-compiled wheels are available on PyPI: pip install qector-decoder-v3
//
// This stub file is present so Cargo.toml resolves correctly when the repo is
// cloned without the private source archive. The CI build injects the real
// src/ tree via the RUST_SRC_B64_1 / RUST_SRC_B64_2 Actions secrets before
// running maturin.
//
// To build locally with the full source, contact: admin@qector.store

compile_error!("The qector-decoder Rust source is proprietary and not distributed \
               via this repository. Install the pre-built wheel from PyPI: \
               `pip install qector-decoder-v3`. To license the source, contact \
               admin@qector.store");
